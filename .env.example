 # Copy this file to .env and modify the values as needed

# Database Configuration
MONGODB_URL=mongodb://admin:btgpactual2025@localhost:27017/btg_funds?authSource=admin
DATABASE_NAME=btg_funds

# Security Configuration
SECRET_KEY=btg_super_secret_key_2025_change_in_production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Application Configuration
ENVIRONMENT=development
LOG_LEVEL=INFO
API_TITLE="BTG Pactual Funds API"
API_VERSION="1.0.0"
API_DESCRIPTION="API para gestión de fondos de inversión BTG Pactual"

# CORS Configuration
ALLOWED_ORIGINS=["http://localhost:3000", "http://localhost:8000", "http://localhost:8080"]

# Email Configuration (AWS SES)
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_REGION=us-east-1
FROM_EMAIL=noreply@btgpactual.com

# SMS Configuration (AWS SNS)
SMS_AWS_ACCESS_KEY_ID=your_sms_aws_access_key
SMS_AWS_SECRET_ACCESS_KEY=your_sms_aws_secret_key
SMS_AWS_REGION=us-east-1

# Development Configuration
DEBUG=True
RELOAD=True

# Docker Configuration (for docker-compose)
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=btgpactual2025
MONGO_INITDB_DATABASE=btg_funds

# MongoDB Express (Database UI)
ME_CONFIG_BASICAUTH_USERNAME=btg
ME_CONFIG_BASICAUTH_PASSWORD=pactual